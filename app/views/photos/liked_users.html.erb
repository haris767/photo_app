<h2>Users who liked this photo</h2>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email, friend_path(user) %> <!-- Adjust if using username or something else -->
    </li>
  <% end %>
</ul>
