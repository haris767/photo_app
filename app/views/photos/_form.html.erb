<%= form_with model: @photo, local: true do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :file %>
  <%= form.file_field :file %>

  <%= form.submit %>
<% end %>
